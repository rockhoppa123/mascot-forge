# Raw external-research responses

Drop the **unedited** answers from Gemini / Perplexity here, one file per tool per run:

- `perplexity-YYYY-MM-DD.md`
- `gemini-YYYY-MM-DD.md`

The prompt to run is in [`../external-research-prompt.md`](../external-research-prompt.md).

Keep these raw (with the tool's own links) — they are the source-of-truth provenance.
Curated conclusions live in `../landscape.md`, `../references.md`, and `../research-log.md`.
